# Linux (apt) packages — installed individually by install.sh so one
# missing package does not abort the whole batch. On older Ubuntu/Debian
# or distros without the universe repo, some packages may not be available
# (e.g. `tealdeer` requires Ubuntu 24.04+ universe). Failures are warned
# but do not stop the installation.

bat
curl
wget
btop
eza
fd-find
fonts-powerline
fzf
gh
git
glow
jq
tealdeer
tmux
vim
zoxide
zsh
